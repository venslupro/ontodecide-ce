# This file is maintained automatically by "terraform init".
# Manual edits may be lost in future updates.
#
# NOTE: cloudflare/cloudflare provider entry is intentionally absent from
# this lock file.  It will be resolved + hashed at CI runtime via
# `terraform init -upgrade` using the ~> 5.19 constraint declared in
# versions.tf.  v5.19+ ships automatic state upgraders that migrate
# v4 state transparently on first plan/apply.
